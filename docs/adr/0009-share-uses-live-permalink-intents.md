# Share uses live Permalink intents

Article Share icons copy the Design Prototype row. Destinations are each network's share of the live Permalink. WordPress.com `?share=` URLs and a copy-link control are out — the prototype has the icons, not copy-link, and `?share=` will not hit WordPress after cutover.
