# Acquisition captures `/pricing` and `/blog`

Marketing landings include `/`, `/pricing`, `/blog` (list and Permalinks), `/github`, `/claw`, `/versus`, and `/comparison`. Login stays out. Visitors who enter via `blog.knowhereto.ai` are captured after redirect onto `/blog`. Claw and Versus stay on the capture list even though they are not restyled.



