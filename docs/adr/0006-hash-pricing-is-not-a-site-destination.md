# `/#pricing` is not a Site destination

The Design Prototype's Header/Footer send Pricing to the Pricing page, not `/#pricing`. The Landing Pricing Block may keep `id="pricing"` as markup. We do not keep `/#pricing` as a nav target or compatibility redirect.
