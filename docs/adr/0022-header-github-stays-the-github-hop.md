# Header GitHub stays the `/github` hop

Site Chrome GitHub goes to `/github` on this Site, which then sends people to the public repo. That hop stays so acquisition still captures `/github`. Brand the interstitial. Do not link Header straight to github.com.
