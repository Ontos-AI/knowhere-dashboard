# Site Chrome keeps Playground

Header Playground stays at `https://notebook.knowhereto.ai` with `ctaId: playground_external`. The Design Prototype omitted it from the text nav; it is live Content, not a 1:1 miss.
