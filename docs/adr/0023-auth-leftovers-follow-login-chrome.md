# Auth leftovers follow Login chrome

Register, forgot-password, reset-password, verify-email, and OAuth/magic-link callback pages use Login’s standalone chrome and Knowhere Brand. Better Auth behavior does not change. `/design-system` stays a dev page.
