# Site Theme is system, light, or dark

Site Theme is one stored choice across Landing, Pricing, Blog, Login, and Dashboard: `system`, `light`, or `dark`. The control is a three-option menu modeled on the language switcher, not a two-way toggle. That picker is an Addition, so it follows Knowhere Brand.

